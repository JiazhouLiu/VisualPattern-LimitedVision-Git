import itertools
r = 12
aList = [15,16]
bList = [6, 7]
matrix = [[0 for x in range(2)] for y in range(2)]
for a in list(aList):
for b in list(bList):
matrix[bList.index(b)][aList.index(a)] = (abs(int(a/r) - int(b/r)) + abs(a%r - b%r))
#print(matrix)
print(min([sum([matrix[p[0]][p[1]] for p in enumerate(perm)]) for perm in itertools.permutations(range(len(matrix)))]))
